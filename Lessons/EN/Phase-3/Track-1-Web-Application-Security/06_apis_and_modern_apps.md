# Track 1 · Stage 1.6 — APIs and Modern Apps

## Learning objectives
- Describe REST verbs, JSON bodies, and common status codes
- Observe API calls from a lab SPA via DevTools or proxy
- Explain token/JWT *concepts* and broken object authorization risks

## Practice
Capture API traffic from Juice Shop (or similar). Optionally run a small `requests` script against the **lab** API only (`Codes/Python/Track-1-Web/` when available).

## Review
- How can an API expose IDOR differently from a classic HTML form?
